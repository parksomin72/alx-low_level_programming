#include "main.h"
/**
 * sub - substract two integers and returns the result
 * @a: first int
 * @b: second int
 * Return: substracted final result
 */
int sub(int a, int b)
{
	int c;

	c = a - b;
	return (c);
}

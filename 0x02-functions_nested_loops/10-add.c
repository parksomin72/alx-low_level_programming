#include "main.h"
/**
 * add - function that adds two integers
 * @number1: int to be added to number2.
 * @number2: int to be added to number1.
 * Return: the result.
 */
int add(int number1, int number2)
{
	int sum;

	sum = number1 + number2;
	return (sum);
}
